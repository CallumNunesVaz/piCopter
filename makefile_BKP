LDLIBS=-lbcm2835
CFLAGS = -Wall -g

MAIN_EXEC = piCopter.run 
SRC = libs/$(wildcard *.c)

default:
	cd libs \
	for name in $(SRC) ; do \
		gcc $(CFLAGS) $$name -o $(LDLIBS) \
		mv *.o ../cli \
		cd ../ \
	done
	cd ../cli \
	gcc $(CFLAGS) main.c -o $(MAIN_EXEC) \

clean:
	rm cli/*.o \

